# Dev/test dependencies. The tests cover the pure logic and do not import the
# transcription backend, so faster-whisper is intentionally omitted here to keep
# CI fast. Install the full runtime from requirements.txt to run the app.
PySide6>=6.6
sounddevice>=0.4
numpy>=1.24
keyboard>=0.13
pytest>=8
ruff>=0.5
