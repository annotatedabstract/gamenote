# Runtime dependencies for gamenote (CPU default).
PySide6>=6.6
sounddevice>=0.4
numpy>=1.24
keyboard>=0.13
faster-whisper>=1.0

# GPU is an opt-in advanced path, not bundled by default. To use CUDA, also:
#   pip install nvidia-cublas-cu12 nvidia-cudnn-cu12
