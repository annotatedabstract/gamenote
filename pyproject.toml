[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
# Defaults (E4/E7/E9 + Pyflakes) plus import-sorting (I), bugbear (B), and
# pyupgrade (UP).
extend-select = ["I", "B", "UP"]

[tool.ruff.format]
# black-compatible; enforced in CI via `ruff format --check`. Run `ruff format`
# to apply.

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-q"
