[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
# Ruff's default rules (E4/E7/E9 + Pyflakes) fit this project well.

[tool.ruff.format]
# black-compatible; run `ruff format` to apply.

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
